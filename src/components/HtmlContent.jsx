const HtmlContent = () => {
  return (
    <div
      style={{
        width: "100%",
        height: "100%",
        backgroundColor: "rgba(0, 0, 0, 0.5)",
      }}
    >
      <p style={{ color: "white", textAlign: "center" }}>
        Este es un componente de React como material
      </p>
    </div>
  );
};

export default HtmlContent;
